import { TimerSubjectDirective } from './timer-subject.directive';

describe('TimerSubjectDirective', () => {
  it('should create an instance', () => {
    const directive = new TimerSubjectDirective();
    expect(directive).toBeTruthy();
  });
});
